import {makeStyles} from "@material-ui/core/styles";

const mainContentStyles = makeStyles((theme) => ({
    root: {
        textAlign:"center",
        marginTop:theme.spacing(25)
    },
}));

export default mainContentStyles;
